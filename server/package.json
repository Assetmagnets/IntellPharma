{
  "name": "server",
  "version": "1.0.0",
  "description": "IntellPharma Backend Server",
  "main": "server.js",
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "dev": "npx -y kill-port 5000 && nodemon server.js",
    "start": "node server.js",
    "restart": "npx -y kill-port 5000 && node server.js",
    "db:push": "npx prisma db push",
    "db:generate": "npx prisma generate",
    "postinstall": "npx prisma generate",
    "test": "echo \"Error: no test specified\" && exit 1",
    "zip": "node scripts/zip_deploy.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "type": "commonjs",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "@prisma/client": "^5.22.0",
    "bcryptjs": "^3.0.3",
    "cors": "^2.8.5",
    "dotenv": "^17.2.3",
    "express": "^5.2.1",
    "jsonwebtoken": "^9.0.3",
    "node-cron": "^4.2.1",
    "nodemailer": "^7.0.12",
    "prisma": "^5.22.0",
    "stripe": "^20.2.0"
  },
  "devDependencies": {
    "adm-zip": "^0.5.16",
    "nodemon": "^3.1.11"
  }
}